.....FF...                                                               [100%]
=================================== FAILURES ===================================
________________ test_mixture_likelihood_and_pickle[spike_slab] ________________

name = 'spike_slab'

    @pytest.mark.parametrize('name', ['spike_slab', 'curved_spike_slab8'])
    def test_mixture_likelihood_and_pickle(name):
        model, _ = cases.PAPER_CASES[name].build()
        restored = pickle.loads(pickle.dumps(model))
        means = np.asarray(cases.SS8_COMPONENT_MEANS)
        covariances = np.asarray(cases.SS8_COMPONENT_COVARIANCES)
        beta = .4 if name == 'curved_spike_slab8' else 0.
        for seed in range(5):
            u = model.sample_U(jax.random.PRNGKey(seed))
            x = np.asarray(jax.tree.leaves(model.transform_to_X(u))[0])
            values = []
            for mean, covariance in zip(means, covariances, strict=True):
                z = x.copy()
                z[1] -= beta * ((x[0]-mean[0])**2-covariance[0, 0])
                values.append(multivariate_normal.logpdf(z, mean, covariance))
            np.testing.assert_allclose(
                model.log_likelihood(u), logsumexp(values), rtol=0, atol=1e-11,
            )
            np.testing.assert_allclose(
>               component_log_likelihoods(x[None, :], means, covariances, beta)[0],
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                values, rtol=0, atol=1e-11,
            )

benchmarks/paper_reproduction/test_protocol.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
benchmarks/paper_reproduction/references.py:70: in component_log_likelihoods
    return np.stack(values, axis=1)  # [N, 2]
           ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

arrays = [array(-29.81101517), array(-10.91986622)], axis = 1, out = None

    @array_function_dispatch(_stack_dispatcher)
    def stack(arrays, axis=0, out=None, *, dtype=None, casting="same_kind"):
        """
        Join a sequence of arrays along a new axis.
    
        The ``axis`` parameter specifies the index of the new axis in the
        dimensions of the result. For example, if ``axis=0`` it will be the first
        dimension and if ``axis=-1`` it will be the last dimension.
    
        Parameters
        ----------
        arrays : sequence of ndarrays
            Each array must have the same shape. In the case of a single ndarray
            array_like input, it will be treated as a sequence of arrays; i.e.,
            each element along the zeroth axis is treated as a separate array.
    
        axis : int, optional
            The axis in the result array along which the input arrays are stacked.
    
        out : ndarray, optional
            If provided, the destination to place the result. The shape must be
            correct, matching that of what stack would have returned if no
            out argument were specified.
    
        dtype : str or dtype
            If provided, the destination array will have this dtype. Cannot be
            provided together with `out`.
    
            .. versionadded:: 1.24
    
        casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'}, optional
            Controls what kind of data casting may occur. Defaults to 'same_kind'.
    
            .. versionadded:: 1.24
    
    
        Returns
        -------
        stacked : ndarray
            The stacked array has one more dimension than the input arrays.
    
        See Also
        --------
        concatenate : Join a sequence of arrays along an existing axis.
        block : Assemble an nd-array from nested lists of blocks.
        split : Split array into a list of multiple sub-arrays of equal size.
        unstack : Split an array into a tuple of sub-arrays along an axis.
    
        Examples
        --------
        >>> import numpy as np
        >>> rng = np.random.default_rng()
        >>> arrays = [rng.normal(size=(3,4)) for _ in range(10)]
        >>> np.stack(arrays, axis=0).shape
        (10, 3, 4)
    
        >>> np.stack(arrays, axis=1).shape
        (3, 10, 4)
    
        >>> np.stack(arrays, axis=2).shape
        (3, 4, 10)
    
        >>> a = np.array([1, 2, 3])
        >>> b = np.array([4, 5, 6])
        >>> np.stack((a, b))
        array([[1, 2, 3],
               [4, 5, 6]])
    
        >>> np.stack((a, b), axis=-1)
        array([[1, 4],
               [2, 5],
               [3, 6]])
    
        """
        arrays = [asanyarray(arr) for arr in arrays]
        if not arrays:
            raise ValueError('need at least one array to stack')
    
        shapes = {arr.shape for arr in arrays}
        if len(shapes) != 1:
            raise ValueError('all input arrays must have the same shape')
    
        result_ndim = arrays[0].ndim + 1
>       axis = normalize_axis_index(axis, result_ndim)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       numpy.exceptions.AxisError: axis 1 is out of bounds for array of dimension 1

/data/albert/miniconda3/envs/jaxns_py/lib/python3.12/site-packages/numpy/_core/shape_base.py:461: AxisError
____________ test_mixture_likelihood_and_pickle[curved_spike_slab8] ____________

name = 'curved_spike_slab8'

    @pytest.mark.parametrize('name', ['spike_slab', 'curved_spike_slab8'])
    def test_mixture_likelihood_and_pickle(name):
        model, _ = cases.PAPER_CASES[name].build()
        restored = pickle.loads(pickle.dumps(model))
        means = np.asarray(cases.SS8_COMPONENT_MEANS)
        covariances = np.asarray(cases.SS8_COMPONENT_COVARIANCES)
        beta = .4 if name == 'curved_spike_slab8' else 0.
        for seed in range(5):
            u = model.sample_U(jax.random.PRNGKey(seed))
            x = np.asarray(jax.tree.leaves(model.transform_to_X(u))[0])
            values = []
            for mean, covariance in zip(means, covariances, strict=True):
                z = x.copy()
                z[1] -= beta * ((x[0]-mean[0])**2-covariance[0, 0])
                values.append(multivariate_normal.logpdf(z, mean, covariance))
            np.testing.assert_allclose(
                model.log_likelihood(u), logsumexp(values), rtol=0, atol=1e-11,
            )
            np.testing.assert_allclose(
>               component_log_likelihoods(x[None, :], means, covariances, beta)[0],
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                values, rtol=0, atol=1e-11,
            )

benchmarks/paper_reproduction/test_protocol.py:120: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
benchmarks/paper_reproduction/references.py:70: in component_log_likelihoods
    return np.stack(values, axis=1)  # [N, 2]
           ^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

arrays = [array(-93.23658612), array(-11.62109571)], axis = 1, out = None

    @array_function_dispatch(_stack_dispatcher)
    def stack(arrays, axis=0, out=None, *, dtype=None, casting="same_kind"):
        """
        Join a sequence of arrays along a new axis.
    
        The ``axis`` parameter specifies the index of the new axis in the
        dimensions of the result. For example, if ``axis=0`` it will be the first
        dimension and if ``axis=-1`` it will be the last dimension.
    
        Parameters
        ----------
        arrays : sequence of ndarrays
            Each array must have the same shape. In the case of a single ndarray
            array_like input, it will be treated as a sequence of arrays; i.e.,
            each element along the zeroth axis is treated as a separate array.
    
        axis : int, optional
            The axis in the result array along which the input arrays are stacked.
    
        out : ndarray, optional
            If provided, the destination to place the result. The shape must be
            correct, matching that of what stack would have returned if no
            out argument were specified.
    
        dtype : str or dtype
            If provided, the destination array will have this dtype. Cannot be
            provided together with `out`.
    
            .. versionadded:: 1.24
    
        casting : {'no', 'equiv', 'safe', 'same_kind', 'unsafe'}, optional
            Controls what kind of data casting may occur. Defaults to 'same_kind'.
    
            .. versionadded:: 1.24
    
    
        Returns
        -------
        stacked : ndarray
            The stacked array has one more dimension than the input arrays.
    
        See Also
        --------
        concatenate : Join a sequence of arrays along an existing axis.
        block : Assemble an nd-array from nested lists of blocks.
        split : Split array into a list of multiple sub-arrays of equal size.
        unstack : Split an array into a tuple of sub-arrays along an axis.
    
        Examples
        --------
        >>> import numpy as np
        >>> rng = np.random.default_rng()
        >>> arrays = [rng.normal(size=(3,4)) for _ in range(10)]
        >>> np.stack(arrays, axis=0).shape
        (10, 3, 4)
    
        >>> np.stack(arrays, axis=1).shape
        (3, 10, 4)
    
        >>> np.stack(arrays, axis=2).shape
        (3, 4, 10)
    
        >>> a = np.array([1, 2, 3])
        >>> b = np.array([4, 5, 6])
        >>> np.stack((a, b))
        array([[1, 2, 3],
               [4, 5, 6]])
    
        >>> np.stack((a, b), axis=-1)
        array([[1, 4],
               [2, 5],
               [3, 6]])
    
        """
        arrays = [asanyarray(arr) for arr in arrays]
        if not arrays:
            raise ValueError('need at least one array to stack')
    
        shapes = {arr.shape for arr in arrays}
        if len(shapes) != 1:
            raise ValueError('all input arrays must have the same shape')
    
        result_ndim = arrays[0].ndim + 1
>       axis = normalize_axis_index(axis, result_ndim)
               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       numpy.exceptions.AxisError: axis 1 is out of bounds for array of dimension 1

/data/albert/miniconda3/envs/jaxns_py/lib/python3.12/site-packages/numpy/_core/shape_base.py:461: AxisError
=============================== warnings summary ===============================
../../../../data/albert/miniconda3/envs/jaxns_py/lib/python3.12/site-packages/tensorflow_probability/python/internal/backend/jax/ops.py:684
  /data/albert/miniconda3/envs/jaxns_py/lib/python3.12/site-packages/tensorflow_probability/python/internal/backend/jax/ops.py:684: DeprecationWarning: jax.core.pytype_aval_mappings is deprecated.
    jax.core.pytype_aval_mappings[onp.ndarray])

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED benchmarks/paper_reproduction/test_protocol.py::test_mixture_likelihood_and_pickle[spike_slab]
FAILED benchmarks/paper_reproduction/test_protocol.py::test_mixture_likelihood_and_pickle[curved_spike_slab8]
2 failed, 8 passed, 1 warning in 5.50s
