# System Tests

System tests exercise composed behavior through supported public objects.
Every test in this directory must trace to a specification under
`docs/design/system_tests/`; ad hoc integration checks remain unit tests until
their composed contract is designed.
